# Android UI automation
Start Appium with the UiAutomator2 driver, set `CGM_APK`, `CGM_TEST_EMAIL`, and `CGM_TEST_PASSWORD`, then run `npm install` and `npm test`. Use a staging account and staging API only. Alert deep-link and physical BLE pairing require a configured emulator notification fixture and BLE simulator respectively.
